import { Router, Request, Response } from 'express'

const router = Router()

/* router.get('/:user', async(req,res)=> await test(req,res))
router.get('/2', async (req,res) => await test2(req,res))
router.get("/model/team", async(req,res) => await getTeamModel(req,res)) */

export default router